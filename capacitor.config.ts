import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '20230603_vue',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
